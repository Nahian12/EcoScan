import React from 'react'
import Layout from '../Layout/Layout';

const Profile = () => {
    return (
      <Layout>
        <h1>Protected Page</h1>
      </Layout>
    );
};

export default Profile;
